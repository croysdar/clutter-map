export enum ResourceType {
    PROJECT = "PROJECT",
    ROOM = "ROOM",
    ORGANIZATIONAL_UNIT = "ORGANIZATIONAL_UNIT",
    ITEM = "ITEM"
}

export enum TimelineActionType {
    CREATE = "CREATE",
    UPDATE = "UPDATE",
    DELETE = "DELETE"
}